# PostgreSQL adapter uses LISTEN/NOTIFY so broadcasts work across multiple
# Puma workers. Requires the same credentials as the app DB.
development:
  adapter: postgresql

test:
  adapter: test

production:
  adapter: solid_cable
  connects_to:
    database:
      writing: cable
  polling_interval: 0.1.seconds
  message_retention: 1.day
